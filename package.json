{
  "name": "slimcontext",
  "version": "0.1.2",
  "description": "Trim your AI coding agent's skill context — keep only the skills a task needs. Local, free, with a token-savings dashboard.",
  "license": "MIT",
  "bin": {
    "slimcontext": "dist/src/cli.js"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist/test/",
    "prepare": "tsc"
  },
  "keywords": [
    "claude-code",
    "skills",
    "context",
    "ai-agents",
    "token",
    "hook",
    "bm25",
    "llm"
  ],
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
